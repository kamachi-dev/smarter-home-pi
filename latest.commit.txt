feat(rooms): fix token-linked room fetching from Supabase and Cloud API

- Add Supabase RLS policy "Tokens can view home rooms" allowing token-linked homes to query rooms
- Update GET /api/rooms on Pi to query Supabase directly for linked home with fallback to cloud API
- Bypass user RLS on smarter-home GET /api/rooms when authenticated with valid x-pi-token
- Verified instant fetch of all 5 rooms (Living Room, Kitchen, Master Bedroom, Dining Room, Garage & Workshop)
