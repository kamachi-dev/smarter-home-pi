feat(camera): stream per-frame camera feed with persistent broadcast & cloud state upsert

- Bind camera sensor directly to `SmarterHomeSync` on frame event so every camera frame is processed and dispatched immediately.
- Maintain persistent Supabase Realtime broadcast channel (`home-camera-${homeId}`).
- Periodically upsert latest annotated frame with face recognition boxes into `home_states` (`camera_feed`) for multi-client synchronization.
- Pass TypeScript compilation and keep all source files under 500 lines.
