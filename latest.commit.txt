fix(camera): eliminate rpicam-vid bcm2835_init gpiomem dependency and restart loops

- Replace legacy rpicam-vid with modern libcamera-vid (--nopreview) which operates cleanly without bcm2835/gpiomem
- Add progressive fallback to v4l2 ffmpeg and high-performance MJPEG frame stream
- Fix restart loop when a camera utility exits quickly without frame output
