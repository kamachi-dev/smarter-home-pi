feat(dashboard): add prominent token configuration card and modal controls

- Add a dedicated "Smarter Home Cloud Link" panel card in the Raspberry Pi controller dashboard with live connection status and active token indicators.
- Add "Configure Token" and top-bar "Link Home Token" triggers to open the token configuration modal.
- Add "Unlink / Clear Token" button inside the modal to easily disconnect the Pi from the cloud.
- Pass TypeScript compilation and keep all source files under 500 lines.
