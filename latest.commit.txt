fix(dashboard): bind camera stream display to Tapo IP camera RTSP feed

- Update dashboard HUD header and FPS badges in `html.ts` and `cameraScript.ts` to display "TAPO IP CAMERA (192.168.68.101) • LIVE RTSP".
- Disable automatic local browser webcam capture in `telemetryScript.ts` to prevent browser media devices from overriding the server Tapo camera stream.
- Re-verify TypeScript build (`npm run build`) and test suite (`npm test`) with 7/7 tests passing.
