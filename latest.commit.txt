fix(camera): hardware compression and broadcast payload boundary protection

- Add `--quality 50` hardware JPEG compression and 10 FPS to `rpicam-vid` and `libcamera-vid` to reduce hardware frame size and CPU utilization.
- Add payload boundary protection (`< 90KB`) and safe error-handling in `sendLiveFrameToSmarterHome` to keep Supabase Realtime broadcast channels permanently connected.
- Pass TypeScript compilation and keep all source files under 500 lines.
