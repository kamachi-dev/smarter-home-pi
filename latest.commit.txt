fix(docker): replace static /dev/gpiomem device mapping with privileged mode

- Remove static /dev/gpiomem host device requirement from docker-compose.yml
- Add privileged: true and /sys volume mount for universal Raspberry Pi 3/4/5 and container support
- Support dynamic hardware GPIO character devices and fallback gracefully if direct memory map is unavailable
