refactor(gpio): replace low-level gpiomem access with standard onoff library and kernel subsystems

- Integrate onoff library for standard user-space GPIO control via sysfs without /dev/gpiomem
- Remove node-dht-sensor dependency and eliminate all /dev/gpiomem memory access
- Refactor GpioManager and TemperatureSensor to use onoff and standard Linux 1-wire subsystems
- Verify clean execution across npm run dev and npm start
