fix(realtime): enable public broadcast authorization in realtime.messages and add auto-recovery

- Configure public RLS policy on `realtime.messages` in Supabase to resolve `CHANNEL_ERROR` when subscribing to Realtime Broadcast channels.
- Add automatic channel subscription recovery with exponential retry in `SmarterHomeSync` upon `CHANNEL_ERROR`.
- Pass TypeScript compilation and keep all source files under 500 lines.
