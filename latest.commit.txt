feat(security): upload detection snapshots to Supabase Storage bucket

- Created 'snapshots' public bucket in Supabase Storage (5MB limit, jpeg/png/webp)
- Added RLS policies: public read, authenticated insert, delete for snapshots bucket
- cameraSyncHandler.ts: sendFirstFrameArrival now uploads detection frame to
  snapshots/{homeId}/{detectionId}.jpg in Supabase Storage before writing log entry
- Stores the public Storage URL in home_states.logs snapshot field instead of
  embedding raw base64 strings (keeps DB lean, images properly accessible via CDN)
- Falls back to base64 inline if Storage upload fails (e.g. offline/quota)
- detectedPersonRecord.firstFrameImage also updated to Storage URL so broadcast
  payload contains the URL rather than a large base64 string
