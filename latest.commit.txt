feat(camera): add permanent token cloud linking and end-to-end live footage streaming

- Add `smarterHomeToken` config support in `src/config/env.ts` with local persistence in `data/hub.config.json`.
- Update `SmarterHomeSync` to stream live processed video frames with face recognition bounding boxes to `/api/pi/camera/live` with `x-pi-token` authentication header.
- Add `GET` and `POST /api/config/token` in Fastify server (`src/server/routes/api.ts`) for managing permanent token settings at runtime.
- Add "Link to Smarter Home" modal and top bar button in Pi dashboard UI (`html.ts` and `modalScript.ts`).
- Ensure all source files adhere to 500-line limits and pass full TypeScript build verification.
