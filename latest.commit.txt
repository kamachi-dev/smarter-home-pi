fix(sync): protect Realtime WebSocket broadcast from race conditions and rate limits

- Add `isCameraChannelSubscribed` and `isChannelConnecting` guards in `SmarterHomeSync` to prevent calling `.send()` while channel handshakes are pending.
- Throttle camera broadcast frame stream to stable ~3.3 FPS and eliminate redundant reconnection loops.
- Pass TypeScript compilation and keep all source files under 500 lines.
