fix(camera): isolate stderr to suppress bcm2835_init notices and streamline camera discovery

- Route camera subprocess stderr through filtered stream parser to suppress non-critical bcm2835_init LED warnings
- Add modern libcamera-vid and v4l2 ffmpeg discovery with graceful MJPEG fallback
- Prevent infinite restart loops if hardware utilities are unconfigured on the host
