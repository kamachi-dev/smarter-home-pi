feat(camera): implement lightweight edge motion detection and targeted event dispatching

- Implemented EdgeMotionDetector using fast temporal luminance frame-differencing (~0.5ms CPU execution time) in src/sensors/camera/motionDetector.ts.
- Replaced continuous frame-by-frame neural face recognition with an edge motion-triggered pipeline in CameraSensor (src/sensors/camera/index.ts).
- CameraSensor only invokes neural inference when significant motion exceeds threshold or when tracking active face presence, dramatically slashing Pi CPU and server load.
- Added sendMotionAlert in CameraSyncHandler (src/sync/cameraSyncHandler.ts) to push motion score and snapshot to Supabase home_states, Realtime broadcast, and /api/pi/telemetry.
- Wired motion_detected event listener in SmarterHomeSync (src/sync/smarterHomeSync.ts) and included motion status in TelemetrySyncHandler (src/sync/telemetrySyncHandler.ts).
- Added unit tests in src/sensors/camera/__tests__/motionDetector.test.ts (24/24 tests passing).
- Verified full TypeScript build (npm run build) and adhered to the 500-line file limit across all files.
