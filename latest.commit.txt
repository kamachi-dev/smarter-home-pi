fix(dashboard): fix client script template interpolation and expose modal functions globally

- Fix template string interpolation in `clientScript.ts` and `html.ts` by removing escaped backslashes (`\${script}` -> `${script}`) so all client JavaScript compiles and runs in the browser.
- Expose `openTokenModal`, `closeTokenModal`, `handleSaveToken`, `handleClearToken`, `openAddSensorModal`, and `openEnrollFaceModal` explicitly on the `window` object.
- Pass TypeScript compilation and keep all source files under 500 lines.
