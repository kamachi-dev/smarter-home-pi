perf(camera): optimize hardware JPEG encoding quality and accelerate state synchronization

- Optimize FrameAnnotator JPEG encoding quality to 50 for efficient, lightweight network transmission under Supabase WebSocket payload limits.
- Accelerate `home_states.camera_feed` upsert frequency to 800ms for continuous streaming fallback.
- Pass TypeScript compilation and keep all source files under 500 lines.
