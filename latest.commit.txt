feat(camera): burn live microsecond timecode into JPEG frames

- Embed live ISO timestamp directly into the hardware camera JPEG HUD overlay in `FrameAnnotator` to visually confirm per-frame video refresh.
- Pass TypeScript compilation and keep all source files under 500 lines.
