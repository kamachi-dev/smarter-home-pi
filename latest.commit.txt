fix(camera): add rtsp_flags prefer_tcp and credential case-insensitive fallbacks for 401 resolution

- Add `-rtsp_flags prefer_tcp` to all TCP capture strategies in `CameraSensor` for reliable Digest authentication challenge negotiation.
- Add username case fallback variants (`CapstoneCam2`, `capstonecam2`, `CapstoneCam`) in capture strategies.
- Add `getRtspStreamUrlWithCustomUser` helper to `TapoCameraService`.
- Re-verify build with `npm run build` (`tsc`) and unit tests with `npm test` (7/7 tests passing).
