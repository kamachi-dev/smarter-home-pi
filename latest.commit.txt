chore(config): update default Tapo camera credentials to CapstoneCam

- Update `tapoCameraUser` default to `CapstoneCam` in `src/config/env.ts` and `.env.example`.
- Update `tapoCameraPassword` default to `OliverSoriano` in `src/config/env.ts` and `.env.example`.
- Update unit tests in `src/sensors/camera/__tests__/tapoClient.test.ts`.
- Verify build with `npm run build` (`tsc`) and unit test suite with `npm test` (7/7 tests passing).
