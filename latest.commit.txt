fix(dashboard): eliminate broken image icon with smooth opacity transition and fallback rendering

- Ensure default camera and temperature sensors are always registered and active
- Fix fallback 640x480 JPEG frame generation to support standard MJPEG streaming
- Add smooth opacity transition to live camera element to eliminate broken image icon during initialization
- Confirm real neural models (TinyFaceDetector, FaceLandmark68, FaceRecognitionNet) loaded successfully
