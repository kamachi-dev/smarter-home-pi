refactor(sync): remove redundant inbound WebSocket channel subscription on Pi publisher

- Remove unused client-side `initCameraBroadcast()` WebSocket subscription on the Pi publisher to prevent false CHANNEL_ERROR/CLOSED reconnect loops.
- Deliver all live hardware camera frames directly through Supabase Realtime Broadcast REST API (`POST /realtime/v1/api/broadcast`) and concurrent `home_states` database state upserts.
- Pass TypeScript compilation with zero errors and maintain all files under 500 lines.
