feat(realtime): add instant direct payload dispatch for Supabase Realtime light toggles

- Connected `handleRoomRecordUpdate` directly to the `postgres_changes` payload on the `rooms` table for zero-latency relay actuation
- Subscribed to `home_states` (`key = 'lights'`) and `lighting_states` in Realtime channel
- Verified clean build and typecheck with `tsc`
