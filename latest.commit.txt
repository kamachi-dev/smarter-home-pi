perf(camera): optimize stream pipeline to eliminate delay and display latest frames

- Add `-avioflags direct`, `-fflags nobuffer+discardcorrupt`, and `-max_delay 500000` to FFmpeg capture pipeline in `CameraSensor` to bypass input/demuxer buffer buildup.
- Reduce probe/analysis limits (`-analyzeduration 1000000`, `-probesize 1000000`) for low-latency live streaming.
- Ensure instantaneous real-time MJPEG chunk dispatching to web clients.
- Validate type safety with `npm run build` and run test suite with `npm test`.

