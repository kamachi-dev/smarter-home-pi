feat(sync): implement Supabase Realtime Broadcast REST delivery

- Leverage Supabase Realtime Broadcast REST API (`POST /realtime/v1/api/broadcast`) for stateless edge-to-cloud frame delivery with zero WebSocket drops.
- Retain dual-stream pattern with direct `home_states.camera_feed` upsert every 800ms.
- Pass TypeScript compilation and keep all source files under 500 lines.
