feat(camera): add auto-scanning across video nodes and detailed hardware diagnostics

- Implement physical /dev/video* device scanning and auto-discovery
- Add sequential hardware probing across rpicam-vid, libcamera-vid, and ffmpeg v4l2 device nodes
- Capture and log camera stderr diagnostics to identify exact hardware detection state
- Revert standby frame generator to clean minimal buffer
