fix(dashboard): resolve camera feed visibility and client script execution in Pi UI

- Fix string escape syntax error in `telemetryScript.ts` to allow client-side initialization and timecode ticker to run immediately.
- Remove `opacity-0` and opaque canvas background in `html.ts` and `cameraScript.ts` so live hardware MJPEG stream `/api/camera/stream` is directly visible with dynamic HUD bounding reticles.
- Pass TypeScript compilation and keep all source files under 500 lines.
