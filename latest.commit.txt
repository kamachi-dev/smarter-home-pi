fix(sync): wait for device token before fetching rooms and hardware pins

- Sequenced dashboard init() to fetchStatus() and resolve permanent token before querying rooms, pins, and faces.
- Added reloadHubToken() to SmarterHomeSync to clear cached home ID, re-query linked home from home_tokens, and re-sync rooms and models.
- Updated POST /api/config/token to reload hub token immediately upon saving.
- Added automatic re-fetch of rooms, pins, and faces in modalScript upon saving or updating token.
- Verified all 24 unit and integration tests pass with npm test and clean tsc compilation with npm run build.
