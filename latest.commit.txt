feat(camera): process each frame with face recognition and render recognition squares

- Add `FrameAnnotator` to process camera frames and render facial recognition bounding boxes, corner reticles, confidence tags, and HUD overlays directly onto video frames.
- Update `FaceRecognitionEngine` to support multi-face detection, landmark extraction, and bounding box arrays with confidence ratings.
- Update `CameraSensor` to run facial recognition on captured/standby/ingested frames and annotate with recognition squares before updating live footage.
- Add `POST /api/camera/frame` and WebSocket `camera_frame` message handling for real-time external and client webcam frame ingestion.
- Split `dashboard.ts` into modular components (`html.ts`, `clientScript.ts`, `telemetryScript.ts`, `cameraScript.ts`, `modalScript.ts`) adhering to the 500-line limit rule.
- Enhance dashboard canvas and live footage loop to capture webcam frames for server AI identification and render dynamic recognition squares.
