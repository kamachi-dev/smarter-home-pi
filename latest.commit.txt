feat(sync): persist active token on Pi and auto-initialize camera broadcast on startup

- Save active home token in `data/hub.config.json` and `.env` on the Raspberry Pi so streaming initializes automatically on boot.
- Auto-subscribe to the home's Supabase Realtime broadcast channel (`home-camera-${homeId}`) upon startup and whenever the token configuration is updated.
- Pass TypeScript compilation and keep all source files under 500 lines.
