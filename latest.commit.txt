fix(camera): correct MJPEG stream boundary and stream chunk parsing

- Fix multipart MJPEG stream boundary header (`boundary=frame`) in `/api/camera/stream` and immediately dispatch latest frame on connection.
- Refactor MJPEG stream chunk parser in `CameraSensor` with byte-aligned SOI (`0xff 0xd8`) and EOI (`0xff 0xd9`) boundary slicing.
- Pass TypeScript compilation and keep all source files under 500 lines.
