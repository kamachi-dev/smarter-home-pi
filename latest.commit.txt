fix(face-api): resolve CommonJS module interop for neural networks and make camera stream always live

- Fix face-api CommonJS module import resolution for TinyFaceDetector and ResNet nets
- Remove manual webcam toggle button and initialize camera stream automatically
- Verify clean neural weights loading and active stream pipeline
